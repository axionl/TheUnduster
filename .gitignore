# Build output
target/
app/node_modules/
app/dist/
app/build/
app/src-tauri/target/
# DMG / bundling output at the repo root
/dist/

# Python / training
training/.venv/
__pycache__/
*.pyc

# Models downloaded at runtime (verified by SHA-256 before use)
*.onnx.download

# Per-roll working state written next to a user's scans
.unduster/

# Local tooling and issue tracking (not part of the public project)
.beads/
.dolt/
*.db
.beads-credential-key
.claude/
.codex/
.agents/
CLAUDE.md
AGENTS.md

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
